# =============================================================================
# Activity Tracker - Environment Configuration
# =============================================================================
# This file contains all environment variables needed for the application.
# Copy this file to create environment-specific configurations:
#   - .env.dev (development)
#   - .env.prod (production)
#   - .env.test (testing)
#
# IMPORTANT: Never commit actual .env files with real credentials to git!
# =============================================================================

# =============================================================================
# Frontend Configuration (Vite)
# =============================================================================
# All frontend environment variables must be prefixed with VITE_

# API Configuration
VITE_API_URL=http://localhost:3000

# Auth0 Configuration
VITE_AUTH_DOMAIN=your-tenant.auth0.com
VITE_AUTH_CLIENT_ID=your-client-id
VITE_AUTH_DATABASE_CONNECTION=Username-Password-Authentication
VITE_AUTH_REDIRECT_URI=http://localhost:4000/callback

# =============================================================================
# Backend Configuration (Node.js)
# =============================================================================

# Server Configuration
BACKEND_PORT=3000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/baaa-hub

# CORS Configuration
CORS_ORIGIN=http://localhost:4000

# Auth0 Configuration (Backend)
# Used for JWT validation
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_AUDIENCE=your-api-audience

# Debug Mode
DEBUG=true

# =============================================================================
# MinIO Object Storage Configuration
# =============================================================================
# MinIO is used for storing user profile images (avatars and banners)
# For local development, MinIO runs via docker-compose on localhost:9000
#
# MinIO Server Configuration
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false

# MinIO Access Credentials
# Default credentials for local development
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# MinIO Bucket Name (for storing images)
MINIO_BUCKET=baaa-hub-images

# MinIO Region (required for S3 compatibility)
MINIO_REGION=us-east-1

# =============================================================================
# Environment Selector
# =============================================================================
# This variable is used by both frontend and backend to select the correct
# environment file. Options: dev, prod, test
# Default: dev
ENV=dev
