# =============================================================================
# BAAA-Hub Production Environment Configuration
# =============================================================================
# Copy this file to .env and configure before deploying
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker compose pull && docker compose up -d
# =============================================================================

# =============================================================================
# Application Settings
# =============================================================================

# Port to expose the application (default: 8080)
APP_PORT=8080

# CORS origin - URL where the application is accessed
# Local: http://localhost:8080
# Production: https://your-domain.com
CORS_ORIGIN=http://localhost:8080

# Debug mode (true/false)
DEBUG=false

# =============================================================================
# Auth0 Configuration (Backend)
# =============================================================================
# Used for JWT validation and webhook authentication

AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_AUDIENCE=your-api-audience

# Webhook secret for Auth0 Post-Login Actions
# Generate with: openssl rand -base64 32
AUTH0_WEBHOOK_SECRET=your-webhook-secret

# =============================================================================
# MinIO Object Storage
# =============================================================================
# Used for storing user profile images (avatars and banners)
#
# IMPORTANT: Change these credentials in production!
# Generate with: openssl rand -base64 32

MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=baaa-hub-images

# =============================================================================
# Frontend Build-time Variables (CI/CD)
# =============================================================================
# NOTE: Frontend variables (VITE_*) are baked into the image at build time
# by GitHub Actions using repository secrets. They are NOT read from this file.
#
# Configure these in GitHub repository secrets:
#   - VITE_AUTH_DOMAIN
#   - VITE_AUTH_CLIENT_ID
#   - VITE_AUTH_REDIRECT_URI
#   - VITE_AUTH_DATABASE_CONNECTION
#   - VITE_API_URL
#
# To update frontend config, update GitHub secrets and trigger a new build.
# =============================================================================
