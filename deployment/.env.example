# =============================================================================
# Production Deployment Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values before deploying
#
# This is the ONLY .env file needed for production deployment
# Environment files are created inside containers at build/runtime to avoid
# permission issues
# =============================================================================

# =============================================================================
# Frontend Build-time Variables
# =============================================================================
# These are baked into the frontend build at Docker build time
# Changes require rebuilding the Docker image: ./deploy.sh build

# Auth0 Configuration
VITE_AUTH_DOMAIN=your-tenant.auth0.com
VITE_AUTH_CLIENT_ID=your_auth0_client_id
VITE_AUTH_REDIRECT_URI=http://localhost:8080/callback
VITE_AUTH_DATABASE_CONNECTION=Username-Password-Authentication

# API Configuration
# Set this to the URL where the application will be accessed
# For local testing: http://localhost:8080
# For production with NPM: https://dev1.pette.dev
VITE_API_URL=http://localhost:8080

# =============================================================================
# Backend Runtime Variables
# =============================================================================
# These are used at container runtime
# Changes take effect after container restart: ./deploy.sh restart

# CORS Configuration
# Should match the URL where the application is accessed
# For local testing: http://localhost:8080
# For production with NPM: https://dev1.pette.dev
CORS_ORIGIN=http://localhost:8080

# Debug Mode
DEBUG=false

# MongoDB Configuration
# When using docker-compose, MongoDB is automatically configured
# The MONGODB_URI is set internally to: mongodb://mongodb:27017/baaa-hub
# For external MongoDB, set this variable:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/baaa-hub

# =============================================================================
# Production Deployment with Nginx Proxy Manager (NPM)
# =============================================================================
# For production deployment to https://dev1.pette.dev:
#
# 1. Update the values:
#    VITE_AUTH_REDIRECT_URI=https://dev1.pette.dev/callback
#    VITE_API_URL=https://dev1.pette.dev
#    CORS_ORIGIN=https://dev1.pette.dev
#
# 2. Configure NPM to point to nginx-prod:8080
#    See: NPM_SETUP_GUIDE.md for detailed instructions
#
# 3. Update Auth0 with the new callback URL
#
# 4. Rebuild and restart:
#    ./deploy.sh build
#    ./deploy.sh start
# =============================================================================
